name: Deploy infrastructure
on:
  push:
    branches:
      - main
      - patent-ci/cd
  workflow_dispatch:
 
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run:  |
          echo "test run"
